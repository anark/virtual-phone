<table class="table table-striped table-hover">
  <%= link_to "Add New Number", new_number_path, :class => "btn" %>
  <thead>
    <tr>
      <th>Number</th>
      <th>To</th>
    </tr>
  </thead>
  <tbody>
    <% @numbers.each do |number| %>
      <tr>
        <td><%= number.number.phony_formatted %></td>
        <td><%= number.phone.number.phony_formatted %></td>
      </tr>
    <% end %>
  </tbody>
</table>
